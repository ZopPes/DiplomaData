<UserControl x:Class="DiplomaData.Tabs.TabsControls.UCTabReviewer"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:DiplomaData.Tabs.TabsControls"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <DataGrid ItemsSource="{Binding Table}" AutoGenerateColumns="False">
        <DataGrid.Columns>
            <DataGridTextColumn Header="Фамилия" Binding="{Binding surname}" />
            <DataGridTextColumn Header="Имя" Binding="{Binding name}" />
            <DataGridTextColumn Header="Отчество" Binding="{Binding patronymic}" />
            <DataGridTextColumn Header="компания" Binding="{Binding company}" />
        </DataGrid.Columns>
    </DataGrid>
</UserControl>