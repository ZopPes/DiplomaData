<Window
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DiplomaData"
        x:Class="DiplomaData.MainWindow"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="800">
    <Window.DataContext>
        <local:Test/>
    </Window.DataContext>
    <Window.Resources>
        <local:Topic x:Key="NewTopic"/>

    </Window.Resources>
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        <Menu IsMainMenu="True">
            <MenuItem Header="главная"/>
            <MenuItem Header="не главная"/>
            <MenuItem Header="памагитэ"/>
        </Menu>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="AUTO"/>
                <ColumnDefinition/>
                <ColumnDefinition MaxWidth="300" Width="auto"/>
            </Grid.ColumnDefinitions>
            <Menu Width="100" >
                <Menu.Resources>
                    <Style TargetType="{x:Type MenuItem}">
                        <Style.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Visibility" Value="Collapsed"/>

                            </Trigger>
                            <Trigger Property="IsEnabled" Value="True">
                                <Setter Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </Menu.Resources>
                <Menu.ItemsPanel>
                    <ItemsPanelTemplate>
                        <VirtualizingStackPanel Orientation="Vertical" />
                    </ItemsPanelTemplate>
                </Menu.ItemsPanel>
                <Expander Header="Добавить">
                    <StackPanel>
                        <TextBox Text="{Binding Title, Source={StaticResource NewTopic}}"/>
                        <TextBox Text="{Binding Description, Source={StaticResource NewTopic}}"/>
                        <CheckBox IsChecked="{Binding Used, Source={StaticResource NewTopic}}"/>
                        <Button Content="ок" Command="{ Binding Topics.LCAdd}"
                                CommandParameter="{Binding Source={StaticResource NewTopic}}"/>
                    </StackPanel>
                </Expander>
                <CheckBox Content="занятые"/>
                <StackPanel Width="75">
                    <Label Content="поиск"/>
                    <TextBox/>
                </StackPanel>
                <MenuItem Command="New"/>
                <MenuItem Command="Copy"/>
                <MenuItem Command="Help"/>
                <MenuItem Command="Cut"/>
                <MenuItem Command="Undo"/>
                <MenuItem Command="SelectAll"/>
                <MenuItem Command="CancelPrint"/>
                <MenuItem Command="Close"/>
                <MenuItem Command="ContextMenu"/>
                <MenuItem Command="CorrectionList"/>
                <MenuItem Command="Delete"/>
                <MenuItem Command="Find"/>
                <MenuItem Command="Open"/>
                <MenuItem Command="Paste"/>
                <MenuItem Command="Print"/>
                <MenuItem Command="PrintPreview"/>
                <MenuItem Command="Properties"/>
                <MenuItem Command="Redo"/>
                <MenuItem Command="Replace"/>
                <MenuItem Command="Save"/>
                <MenuItem Command="SaveAs"/>
                <MenuItem Command="Undo"/>

            </Menu>
            <Grid Grid.Column="1">
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition Height="AUTO"/>
                </Grid.RowDefinitions>
                <TabControl>
                    <TabItem   Header="список тем">
                        <local:UCTopic/>
                    </TabItem>
                    <TabItem Header="список дипломов">
                        <local:UCDiplomas />
                    </TabItem>
                    <TabItem Header="отчёты">
                        <StackPanel Background="Black" >
                            <Button x:Name="button1" Width="100" Height="30" Content="Кнопка 1"/>
                        </StackPanel>
                    </TabItem>
                    <TabItem Header="учащиеся"/>
                    <TabItem Header="люди"/>
                </TabControl>
                <StatusBar Grid.Row="1">
                    <StatusBarItem Content="ww" />
                    <StatusBarItem Content="12312" />
                    <StatusBarItem Content="12312" />
                </StatusBar>

            </Grid>
            <GridSplitter ShowsPreview="False" Width="3" Grid.Column="1"/>
            <Rectangle Grid.Column="3" Width="100"/>
        </Grid>
    </Grid>
</Window>