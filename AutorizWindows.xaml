<Window x:Class="DiplomaData.AutorizWindows"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:DiplomaData"
        mc:Ignorable="d"
        Title="AutorizWindows" Height="250" Width="350"
        FontSize="24" FontWeight="Bold">
    <Grid VerticalAlignment="Center" Margin="30">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="auto"/>
            <ColumnDefinition/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>
        <TextBlock Text="Логин:"/>
        <TextBlock Text="Пароль:" Grid.Row="1"/>
        <TextBox x:Name="login" Grid.Column="1"/>
        <PasswordBox x:Name="password" Grid.Column="1" Grid.Row="1"/>
        <Button Content="войти" Grid.ColumnSpan="2" Grid.Row="2"
                Click="Autoriz_Click"/>
    </Grid>
</Window>
