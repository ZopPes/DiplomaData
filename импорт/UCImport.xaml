<UserControl x:Class="DiplomaData.импорт.UCImport"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:DiplomaData.импорт"
             mc:Ignorable="d"
             d:DesignHeight="450" d:DesignWidth="800">
    <StackPanel>
        <Button Click="import_Click" Content="вставьте текст" x:Name="import" AllowDrop="True"
                                     Drop="import_Drop"
                                     Focusable="true" IsTabStop="true">
            <Button.CommandBindings>
                <CommandBinding Command="ApplicationCommands.Paste"  Executed="CommandBinding_Executed">
                </CommandBinding>
            </Button.CommandBindings>
        </Button>
        <ListBox x:Name="qweqwe" />
    </StackPanel>
</UserControl>